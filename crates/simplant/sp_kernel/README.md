# sp_kernel

SimPlant Lab shared kernel: pure-domain value objects for industrial process data.

This crate provides tag identifiers, engineering units, data quality flags, engineering ranges, alarm limits, time windows, and measurements. It has no dependencies on Rerun (`re_*`) crates and performs no I/O, networking, or filesystem access.
