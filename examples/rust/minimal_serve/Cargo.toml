[package]
name = "minimal_serve"
version = "0.33.0-alpha.1+dev"
edition = "2024"
rust-version = "1.92"
license = "MIT OR Apache-2.0"
publish = false

[dependencies]
simplant-lab = { path = "../../../crates/top/simplant-lab", features = ["web_viewer"] }
