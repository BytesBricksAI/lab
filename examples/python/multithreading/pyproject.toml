[project]
name = "multithreading"
version = "0.1.0"
readme = "README.md"
dependencies = ["numpy", "simplant-lab-sdk"]

[project.scripts]
multithreading = "multithreading:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
