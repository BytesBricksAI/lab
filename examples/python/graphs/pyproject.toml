[project]
name = "graphs"
version = "0.1.0"
readme = "README.md"
dependencies = ["simplant-lab-sdk"]

[project.scripts]
graphs = "graphs:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
